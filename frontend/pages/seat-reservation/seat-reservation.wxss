/* seat-reservation.wxss */
.container {
  padding: 50rpx 30rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 80vh;
}

.page-title {
  font-size: 36rpx;
  font-weight: bold;
  margin-bottom: 30rpx;
  color: var(--primary-color);
}

.page-description {
  font-size: 28rpx;
  color: var(--font-color-light);
} 